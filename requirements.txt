# Quantum Test Selection Pipeline Requirements
# ============================================

# Core dependencies (required)
numpy>=1.20.0
scipy>=1.7.0
pennylane>=0.28.0

# Optional dependencies (for coverage collection)
pytest>=7.0.0          # For pytest-based coverage collection
coverage>=6.0          # For Python coverage analysis

# Optional dependencies (for visualization)
matplotlib>=3.5.0      # For plotting results
pandas>=1.3.0          # For data analysis

# Development dependencies (optional)
jupyterlab>=3.0.0      # For interactive notebooks

